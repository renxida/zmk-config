/* source keypos definitions */
#include "zmk-helpers/key-labels/34.h"

/* activate bluetooth */
#define CONFIG_WIRELESS 1

/* source the main keymap */
#include "base.keymap"

/* use default transform for cradio */
/ { chosen { zmk,matrix_transform = &default_transform; }; };
